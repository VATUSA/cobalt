meta {
  name: Create Policy Category
  type: http
  seq: 2
}

post {
  url: {{hostname}}/policy/category
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Test Category",
    "sort_order": 0
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
