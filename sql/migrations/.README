migrations are managed with golang-migrate

Command to create migrations:

migrate create -ext sql -dir sql/migrations -ts create_users_table

Use -ts (timestamped) naming so parallel branches don't collide on the same
version number. Timestamped migrations sort after the existing -seq files,
since golang-migrate orders by numeric version.

Command to generate db models:
sqlc generate
