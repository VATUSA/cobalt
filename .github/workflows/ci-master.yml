name: CI to Staging
on:
  push:
    branches: [main]
jobs:
  common-ci:
    uses: vatusa/gitops/.github/workflows/common-ci.yml@main
    with:
      image-name: 'cobalt'
      kustomization-folder: 'cobalt'
    secrets: inherit

